---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: frequis.freqtrade.io
spec:
  group: freqtrade.io
  names:
    kind: FreqUI
    listKind: FreqUIList
    plural: frequis
    singular: frequi
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FreqUI is the Schema for the frequis API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FreqUISpec defines the desired state of FreqUI
            properties:
              host:
                description: Host is the hostname for the FreqUI ingress
                type: string
              image:
                description: Image is the FreqUI Docker image to use
                type: string
              ingressAnnotations:
                additionalProperties:
                  type: string
                description: IngressAnnotations are annotations to add to the FreqUI
                  ingress
                type: object
              tls:
                description: TLS configuration for the FreqUI ingress
                items:
                  description: IngressTLS describes the transport layer security associated
                    with an ingress.
                  properties:
                    hosts:
                      description: |-
                        hosts is a list of hosts included in the TLS certificate. The values in
                        this list must match the name/s used in the tlsSecret. Defaults to the
                        wildcard host setting for the loadbalancer controller fulfilling this
                        Ingress, if left unspecified.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    secretName:
                      description: |-
                        secretName is the name of the secret used to terminate TLS traffic on
                        port 443. Field is left optional to allow TLS routing based on SNI
                        hostname alone. If the SNI host in a listener conflicts with the "Host"
                        header field used by an IngressRule, the SNI host is used for termination
                        and value of the "Host" header is used for routing.
                      type: string
                  type: object
                type: array
              tradeBotRefs:
                description: TradeBotRefs is a list of TradeBot names that this FreqUI
                  should manage
                items:
                  type: string
                type: array
            type: object
          status:
            description: FreqUIStatus defines the observed state of FreqUI
            properties:
              message:
                description: Message is a human-readable message indicating details
                  about the current phase
                type: string
              phase:
                description: Phase is the current phase of the FreqUI deployment
                type: string
              url:
                description: URL is the URL where FreqUI is accessible
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
