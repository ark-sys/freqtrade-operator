---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: orders.freqtrade.io
spec:
  group: freqtrade.io
  names:
    kind: Order
    listKind: OrderList
    plural: orders
    singular: order
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Order is the Schema for the orders API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrderSpec defines the desired state of Order configuration
            properties:
              flow:
                description: Flow contains configuration for order flow processing
                properties:
                  cache_size:
                    type: integer
                  imbalance_ratio:
                    type: number
                  imbalance_volume:
                    type: integer
                  max_candles:
                    type: integer
                  scale:
                    type: number
                  stacked_imbalance_range:
                    type: integer
                type: object
              time_in_force:
                description: TimeInForce contains configuration for order time in
                  force options
                properties:
                  entry:
                    type: string
                  exit:
                    type: string
                type: object
              types:
                description: Types contains configuration for different order types
                properties:
                  emergency_exit:
                    type: string
                  entry:
                    type: string
                  exit:
                    type: string
                  force_entry:
                    type: string
                  force_exit:
                    type: string
                  stoploss:
                    type: string
                  stoploss_on_exchange:
                    type: boolean
                  stoploss_on_exchange_interval:
                    type: integer
                  stoploss_on_exchange_limit_ratio:
                    type: number
                type: object
            type: object
          status:
            description: OrderStatus defines the observed state of Order
            properties:
              message:
                type: string
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
