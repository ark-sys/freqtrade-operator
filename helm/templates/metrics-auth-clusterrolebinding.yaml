{{- if and .Values.rbac.create .Values.controllerManager.metrics.enabled -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "freqtrade-operator.fullname" . }}-metrics-auth-rolebinding
  labels:
    {{- include "freqtrade-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "freqtrade-operator.fullname" . }}-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: {{ include "freqtrade-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
