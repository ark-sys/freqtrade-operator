{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "freqtrade-operator.fullname" . }}-manager-rolebinding
  labels:
    {{- include "freqtrade-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "freqtrade-operator.fullname" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "freqtrade-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }} 